# Environment variable files — NEVER commit these
.env
.env.local
.env.*.local
.env.development
.env.production

# Vercel
.vercel

# Node
node_modules/

# Claude Code per-user settings (permissions are personal, not team-wide)
.claude/settings.local.json
.claude/tmp/
