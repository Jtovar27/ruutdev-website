{
  "name": "ruutdev-website",
  "version": "1.0.0",
  "description": "RuutDev website v2 — serverless API functions",
  "private": true,
  "type": "module",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev": "vercel dev",
    "lint": "node scripts/check.mjs",
    "typecheck": "node --check assets/js/site-config.js && node --check assets/js/rebuild.js && node --check api/contact.js",
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2",
    "stripe": "^17"
  }
}
